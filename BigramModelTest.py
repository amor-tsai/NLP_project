# each module is a file name, if you want to import class from that file, u should use this from module import class-name
from BigramModel import BigramModel

model = BigramModel("name",".","./*.txt")
